# 统一按 LF 存储，Windows 批处理保留 CRLF
* text=auto eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制文件不做行尾转换、不参与 diff
*.png binary
*.ico binary
*.jpg binary
*.zip binary
*.exe binary

# 生成物与锁文件在 diff 里折叠
package-lock.json linguist-generated=true
dist/** linguist-generated=true
